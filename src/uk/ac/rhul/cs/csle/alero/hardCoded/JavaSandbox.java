package uk.ac.rhul.cs.csle.alero.hardCoded;

public class JavaSandbox {
  // public JavaSandbox() throws AleroException {
  //
  // // Mesh mesh = new Cuboid(50, 60, 70);
  // // Mesh mesh = new Tetrahedron(50, 50, 40);
  // // AleroMesh mesh = new AleroMesh("stlin/rch12TonWagonFullPaintTestJohnstone.stl");
  // // Mesh mesh = new LOM(new float[] { 0, 0, 0, 30, 0, 0, 30, 30, 0, 0, 30, 0 }, new float[] { 10, 9, 15, 0, 0, 15 }); // LOM in XY
  // // Mesh mesh = new LOM(new float[] { 0, 0, 0, 0, 0, 30, 0, 30, 30, 0, 30, 0 }, new float[] { 10, 0, 0, 20, 5, 0 }); // LOM in YZ
  // // showMesh(mesh);
  //
  // // new TriangleExperiments();
  // new PrintonOnSea().createWorld();
  //
  // // // Start of CSG testing
  // // LOM lomA = new LOM(new float[] { 0, 0, 0, 30, 0, 0, 30, 30, 0, 0, 30, 0 }, new float[] { 0, 0, 30 });
  // // // System.out.println("lomA:\n" + lomA);
  // // LOM lomB = new LOM(new float[] { //
  // // -15, -15, -15, //
  // // 15, -15, -15, //
  // // 15, 15, -15, //
  // // -15, 15, -15 }, new float[] { 0, 0, 30 });
  // //
  // // // showMesh(lomA);
  // // // showMesh(lomB);
  // //
  // // CSG csgA = new CSG(lomA);
  // // // System.out.println("csgA:\n" + csgA);
  // // // AleroMesh meshA = new AleroMesh(csgA);
  // // // showMesh(meshA);
  // //
  // // CSG csgB = new CSG(lomB);
  // // // System.out.println("csgB: " + csgB);
  // // // AleroMesh meshB = new AleroMesh(csgB);
  // // // showMesh(meshB);
  // //
  // // CSG csgMesh = new CSG(mesh);
  // // CSG csgC = csgMesh.difference(csgMesh);
  // // // System.out.println("csgC: " + csgC);
  // // AleroMesh cMesh = new AleroMesh(csgC);
  // // showMesh(cMesh);
  // }
  //
  // void showMesh(Mesh mesh) {
  // MeshView meshView = new MeshView(mesh);
  // meshView.setDrawMode(DrawMode.FILL);
  // PhongMaterial material = new PhongMaterial();
  // material.setDiffuseMap(new Image("file:GERTank.png"));
  // meshView.setMaterial(material);
  // meshView.setCullFace(CullFace.NONE);
  // Alero.add(meshView);
  // }
}
